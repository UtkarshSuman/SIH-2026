export default function RiskInfo({ location }) {
  if (!location) return <aside className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-sm font-bold uppercase tracking-[2px] text-emerald-700">Risk Information</p><h2 className="mt-3 text-2xl font-bold text-slate-950">Select a zone</h2><p className="mt-3 leading-7 text-slate-600">Choose a circular zone or search the live database to view its latest assessment.</p></aside>;
  const badge = location.zoneColor === "RED" ? "bg-red-50 text-red-700" : location.zoneColor === "YELLOW" ? "bg-amber-50 text-amber-700" : "bg-emerald-50 text-emerald-700";
  return <aside className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-sm font-bold uppercase tracking-[2px] text-emerald-700">Database Assessment</p><h2 className="mt-3 text-2xl font-bold text-slate-950">{location.name}</h2><div className={`mt-6 rounded-xl p-4 ${badge}`}><p className="text-xs font-bold uppercase tracking-wider">Risk Level</p><p className="mt-1 text-2xl font-bold">{location.zoneColor} · {(location.worstScore * 100).toFixed(0)}%</p></div><div className="mt-5 space-y-4 text-sm"><div><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">Primary Hazard</p><p className="mt-1 font-semibold text-slate-800">{location.worstHazard || "Not assessed"}</p></div><div><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">Affected Population</p><p className="mt-1 font-semibold text-slate-800">{location.population.toLocaleString()} people</p></div><div><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">Last Updated</p><p className="mt-1 font-semibold text-slate-800">{location.lastAssessedAt ? new Date(location.lastAssessedAt).toLocaleString() : "Awaiting live assessment"}</p></div></div></aside>;
}
