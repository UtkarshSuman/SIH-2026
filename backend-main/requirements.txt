# Rescue Arc Backend — Production Dependencies
# Compatible with Python 3.10, 3.11, 3.12
# Tested with Supabase PostgreSQL, Google Gemini 1.5 Flash, and HuggingFace MiniLM

# ── Core Framework & Server ───────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1

# ── PostgreSQL & Supabase Database ────────────────────────────────────────────
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.30

# ── Alert Service & Notifications ─────────────────────────────────────────────
firebase-admin>=6.5.0
qrcode>=7.4.2
pillow>=10.4.0

# ── RAG System (LangChain + Gemini + Local Embeddings) ────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0
google-generativeai>=0.8.0
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0
pypdf>=5.0.0

# ── Vector Stores (ChromaDB local + Supabase pgvector) ────────────────────────
langchain-chroma>=0.1.4
chromadb>=0.5.0

# RAG System — Python Dependencies (all free, no paid services)
# Install with:
#   pip install -r requirements_rag.txt

# ── Google Gemini LLM (free tier: 15 req/min, 1M tokens/day) ─────────────────
langchain-google-genai>=2.0.0
google-generativeai>=0.8.0

# ── Local ChromaDB vector store (SQLite-backed, no cloud needed) ─────────────
langchain-chroma>=0.1.0
chromadb>=0.5.0

# ── Local HuggingFace embeddings (all-MiniLM-L6-v2, ~22MB, no API key) ───────
langchain-huggingface>=0.1.0
sentence-transformers>=3.3.0

# ── LangChain core packages ───────────────────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# ── PDF document ingestion ────────────────────────────────────────────────────
pypdf>=4.0.0

# ── Pydantic v2 settings (for app/core/config.py) ────────────────────────────
pydantic-settings>=2.0.0

# ── Database & server ────────────────────────────────────────────────────────
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0